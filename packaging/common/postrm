#!/bin/sh
set -e
if [ -d /run/systemd/system ]; then
    systemctl daemon-reload || true
fi
# Purge (not plain remove) also drops stored incident history & config.
if [ "$1" = "purge" ]; then
    rm -rf /var/lib/server-sentinel /etc/server-sentinel
fi
exit 0
